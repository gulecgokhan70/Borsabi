# Master Trader - Project Instructions

## Overview
Turkish trading simulation/education platform (BIST + Crypto). Not real trading - educational/simulation only. Disclaimer: "yatırım tavsiyesi değildir".

## Tech Stack
- App Router, Prisma + PostgreSQL, NextAuth, TailwindCSS, Framer Motion
- Yahoo Finance (via `@/lib/yahoo-finance`) + Midas API for BIST data
- LLM API for AI assistant

## Visual Style - iOS Liquid Glass Theme
- **Body background**: Gradient `linear-gradient(160deg, #0B1120, #0F172A, #111D35, #0B1120)` with `background-attachment: fixed`
- **Glass utility classes** (defined in globals.css):
  - `.glass` - Base glassmorphism: `backdrop-filter: blur(16px)`, semi-transparent white bg/border
  - `.glass-card` - Card-level: `backdrop-filter: blur(20px)`, `rgba(255,255,255,0.04)` bg, `rgba(255,255,255,0.08)` border
  - `.glass-sidebar` - Sidebar: `backdrop-filter: blur(24px)`, slightly more opaque
  - `.glass-nav` - Mobile header nav: `backdrop-filter: blur(12px)`
  - `.glass-inner` - Inner/nested cards: `rgba(255,255,255,0.02)` bg
  - `.glass-btn`, `.glass-hover`, `.glass-active` - Interactive elements
  - `.glass-divider` - Subtle divider lines
  - `.glow-blue`, `.glow-subtle` - Glow effects
- **Border pattern**: `border-white/[0.06]` for separators, `border-white/[0.08]` for card borders
- **Hover pattern**: `hover:bg-white/[0.04]` or `hover:bg-white/[0.06]`
- **Colors**: Primary blue `#3B82F6`, green `#22C55E`, red `#EF4444`, amber `#F59E0B`, text `white`/`#94A3B8`/`#64748B`

## Tier System (2 tiers only)
- **Free**: 5 alarms, basic screening, 3 backtest/day, standard indicators
- **Pro**: 50 alarms, algo scanning, unlimited backtest, strategy builder, advanced indicators, priority AI, API access, custom strategies
- DB `tier` field: 'free' or 'pro' (legacy 'elite' → treated as 'pro' in UI)

## Key Technical Notes
- Prisma import: `import { prisma } from '@/lib/db'` (named export)
- Yahoo Finance: `import { yf } from '@/lib/yahoo-finance'`
- TradeModal uses `marketType` prop, NOT `yahooSymbol`
- Index symbols (XU100.IS, XU030.IS) skip Midas, use Yahoo Finance directly
- All pages use Turkish locale, Turkish characters must be correct
- 18+ pages: dashboard, portfolio, day-trading, swing-trading, ai-assistant, screening, risk-center, trade-log, watchlist, academy, backtest, algo-scan, strategy-builder, leaderboard, alerts, achievements, aksam-analizi, profile

## Auth
- Admin: john@doe.com / johndoe123
- Login/signup pages also use glass styling

## Important Patterns
- All `bg-[#1E293B]` replaced with `glass-card`
- All `bg-[#0F172A]/50` replaced with `glass-inner`
- All `border-[#334155]` replaced with `border-white/[0.08]`
- Sidebar uses `glass-sidebar`, mobile header uses `glass-nav`
