{
  "name": "borsabi-android-packaging",
  "version": "0.1.0",
  "private": true,
  "description": "Reproducible, unsigned TWA preparation for BorsaBi Trader",
  "scripts": {
    "prepare:preview": "node scripts/prepare.mjs --preview",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=22" },
  "dependencies": { "@bubblewrap/core": "1.25.0" }
}
